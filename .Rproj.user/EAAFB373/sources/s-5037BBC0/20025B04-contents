---
title: "Pliki_CSV"
author: "RW"
date: "11 02 2020"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

1. Katalog roboczy
```{r}
# Pobieranie ścieżki bezwzględnej prowadzącej do bieżącego katalogu roboczego.
getwd()
```



```{r}
# Wczytywanie danych z pliku my_file.csv do ramki danych my_df.
my_df <- read.csv("R_plik_CSV.csv", stringsAsFactors = FALSE)

# pobieranie danych z plików CSV z podaniem ściężki dostępu
# my_data <- read.csv("data/my-data.csv", stringsAsFactors = FALSE)
```

Funkcja zwracająca folder w którym instalowane będą dalsze pakiety
```{r}
.libPaths()
```

